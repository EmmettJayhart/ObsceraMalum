{
    "Group": "${modGroup}.${modId}",
    "Name": "${modName}",
    "Version": "${modVersion}",
    "Description": "${modDescription}",
    "Authors": [
        {
            "Name": "${modAuthor}",
            "Email": "${modAuthorEmail}",
            "Url": "${modAuthorUrl}"
        }
    ],
    "Website": "${modUrl}",
    "ServerVersion": ">=${hytaleVersion}",
    "Dependencies": {
        "HyKotlin:HyKotlin": ">=${hykotlinVersion}"
    },
    "OptionalDependencies": {},
    "DisabledByDefault": false,
    "IncludesAssetPack": false,
    "Main": "${modGroup}.${modId}.${modMain}"
}
